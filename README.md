# 美股定时筛选系统

一个基于Python的美股定时筛选系统，支持多周期K线数据采集、技术指标计算、可插拔策略引擎和自动化报告生成。

## ⚠️ 重要更新：数据源升级

**遇到限流问题？** 系统现已支持多个数据源，彻底解决Yahoo Finance限流问题！

📖 **快速解决**: 
- 如果你遇到 "Too Many Requests" 或 "Rate limited" 错误
- 查看 [UPGRADE_NOTES.md](UPGRADE_NOTES.md) 立即切换到更稳定的数据源
- 推荐使用 Alpha Vantage（免费，5分钟配置完成）

```bash
# 使用配置向导快速设置
python3 configure_datasource.py
```

## 功能特性

- **多数据源支持**: Yahoo Finance、Alpha Vantage、Polygon.io
  - ✅ 解决限流问题
  - ✅ 免费API可用
  - ✅ 一键切换
- **多股票池**: 支持S&P 500、NASDAQ 100或自定义股票列表
- **多周期分析**: 支持日K、周K、月K线数据同时分析
- **丰富的技术指标**: MA/EMA、MACD、RSI、布林带、ATR、成交量分析等
- **可插拔策略引擎**: 
  - 均线突破策略
  - 成交量异常策略
  - 价格突破策略
  - RSI超卖/超买策略
  - 支持自定义策略扩展
- **Level 1 硬筛选系统**:
  - 流动性过滤
  - 数据质量过滤
  - 趋势过滤
  - **周趋势确认过滤** ⭐ NEW（多时间框架验证）
  - 综合评分引擎
- **自动化调度**: 支持定时任务，可在美股收盘后和开盘前自动筛选
- **报告生成**: 生成CSV/Excel/JSON格式的筛选报告
- **增量更新**: 智能增量数据更新机制，节省时间和网络资源

## 系统架构

```
stock_screener/
├── main.py                    # 主程序入口
├── configure_datasource.py    # 数据源配置向导
├── check_config.py            # 配置验证工具
├── requirements.txt           # 依赖包
├── config/
│   └── config.yaml           # 配置文件
├── src/
│   ├── __init__.py
│   ├── data_fetcher.py       # 数据采集模块（多数据源）
│   ├── data_fetcher_multi.py # 多数据源实现
│   ├── data_storage.py       # 数据存储模块
│   ├── indicators.py         # 技术指标计算
│   ├── scheduler.py          # 定时调度器
│   ├── reporter.py           # 报告生成器
│   ├── config_manager.py     # 配置管理
│   ├── utils.py              # 工具函数
│   ├── scoring_engine.py     # 评分引擎
│   ├── filters/              # 筛选过滤器模块 ⭐ NEW
│   │   ├── __init__.py
│   │   ├── base_filter.py          # 过滤器基类
│   │   ├── liquidity_filter.py     # 流动性过滤
│   │   ├── data_quality_filter.py  # 数据质量过滤
│   │   ├── trend_filter.py         # 趋势过滤
│   │   └── weekly_trend_filter.py  # 周趋势确认 ⭐ NEW
│   ├── strategy/             # 策略引擎
│   │   ├── __init__.py
│   │   ├── base_strategy.py
│   │   ├── builtin_strategies.py
│   │   └── custom_strategies.py
│   └── trend_scoring/        # 趋势评分
│       ├── __init__.py
│       ├── base_scorer.py
│       └── ma_adx_scorer.py
├── tests/                    # 测试工具集 ⭐
│   ├── README.md            # 测试工具说明
│   ├── test_data_source.py  # 数据源测试
│   ├── debug_alphavantage.py # Alpha Vantage诊断
│   └── test_*.py            # 其他测试工具
├── docs/                     # 文档目录
│   └── DATA_SOURCE_GUIDE.md # 数据源详细指南
├── data/                     # 数据存储目录
│   ├── daily/
│   ├── weekly/
│   └── monthly/
├── reports/                  # 报告输出目录
└── logs/                     # 日志目录
```

## 快速开始

### 0. 配置数据源（推荐先做）

如果你担心限流问题或想要更稳定的数据源：

```bash
# 使用交互式配置向导
python3 configure_datasource.py

# 或手动编辑 config/config.yaml
```

详见 [数据源配置指南](docs/DATA_SOURCE_GUIDE.md)

### 1. 安装依赖

```bash
pip install -r requirements.txt
```

### 2. 配置系统

编辑 `config/config.yaml` 文件，根据需要调整配置：

```yaml
# 数据配置
data:
  stock_universe: sp500    # 股票池: sp500, nasdaq100
  history_days: 180        # 历史数据天数

# 策略配置
strategies:
  ma_crossover:
    enabled: true          # 启用均线交叉策略
    short_period: 20
    long_period: 50
  
  volume_surge:
    enabled: true          # 启用成交量异常策略
    surge_multiplier: 2.0

# 调度配置
scheduler:
  schedules:
    - time: "16:30"        # 美股收盘后
      timezone: "US/Eastern"
    - time: "09:00"        # 美股开盘前
      timezone: "US/Eastern"
```

### 3. 初始化数据

首次使用需要下载历史数据：

```bash
python main.py --init
```

这将下载S&P 500所有成分股的日K、周K、月K数据（可能需要30-60分钟）。

### 4. 运行方式

#### 手动执行一次筛选

```bash
python main.py --run-once
```

#### 启动定时任务

```bash
python main.py --daemon
```

系统将在配置的时间点自动执行筛选任务。

#### 仅更新数据

```bash
python main.py --update
```

## 使用说明

### 查看筛选报告

筛选报告保存在 `reports/` 目录下，文件名包含时间戳：

- `stock_screening_20231201_163000.csv` - CSV格式
- `stock_screening_20231201_163000.xlsx` - Excel格式

Excel报告包含多个sheet，每个策略一个sheet，便于分类查看。

### 添加自定义策略

在 `src/strategy/custom_strategies.py` 中添加您的策略：

```python
from .base_strategy import BaseStrategy

class MyStrategy(BaseStrategy):
    def __init__(self, config: Dict = None):
        super().__init__("MyStrategy", config)
    
    def scan(self, data: Dict[str, pd.DataFrame]) -> List[Dict]:
        results = []
        
        for ticker, df in data.items():
            latest = self.get_latest_row(df)
            
            # 您的筛选逻辑
            if latest['close'] > latest['sma_50']:
                results.append({
                    'ticker': ticker,
                    'signal': '自定义信号',
                    'price': latest['close'],
                    'details': {}
                })
        
        return results
```

然后在 `config/config.yaml` 中启用：

```yaml
strategies:
  my_strategy:
    enabled: true
```

### 技术指标说明

系统自动计算以下技术指标：

| 指标类型 | 指标名称 | 说明 |
|---------|---------|------|
| 移动平均 | SMA_5/10/20/50/200 | 简单移动平均线 |
| 移动平均 | EMA_12/26 | 指数移动平均线 |
| 动量指标 | RSI | 相对强弱指标 |
| 趋势指标 | MACD | 平滑异同移动平均线 |
| 波动指标 | Bollinger Bands | 布林带（上轨、中轨、下轨） |
| 波动指标 | ATR | 平均真实波幅 |
| 成交量 | Volume MA | 成交量移动平均 |
| 成交量 | Volume Ratio | 成交量比率 |

### 策略说明

#### 1. 均线交叉策略 (MA Crossover)

检测短期均线向上突破长期均线（金叉），并可选成交量确认。

**适用场景**: 趋势跟踪，捕捉中长期趋势启动

**配置参数**:
- `short_period`: 短期均线周期（默认20）
- `long_period`: 长期均线周期（默认50）
- `volume_confirm`: 是否需要成交量确认（默认true）

#### 2. 成交量异常策略 (Volume Surge)

检测成交量异常放大且价格上涨的股票。

**适用场景**: 捕捉突发热点，主力资金关注

**配置参数**:
- `lookback_period`: 回看周期（默认20）
- `surge_multiplier`: 成交量倍数阈值（默认2.0）
- `min_price_change`: 最小涨幅百分比（默认2.0%）

#### 3. 价格突破策略 (Breakout)

检测价格突破近期高点的股票。

**适用场景**: 捕捉突破行情，强势股追踪

**配置参数**:
- `lookback_period`: 回看周期（默认20）
- `volume_confirm`: 是否需要成交量确认（默认true）
- `min_volume_ratio`: 最小成交量比率（默认1.2）

#### 4. RSI策略

检测RSI超卖反弹或超买回落机会。

**适用场景**: 短线博弈，抄底逃顶

**配置参数**:
- `oversold_threshold`: 超卖阈值（默认30）
- `overbought_threshold`: 超买阈值（默认70）
- `mode`: 模式（oversold/overbought）

## 部署建议

### 本地Mac部署

适合个人使用，需要保持电脑开机：

```bash
# 使用nohup后台运行
nohup python main.py --daemon > /dev/null 2>&1 &
```

### 云服务器部署

推荐使用云服务器（AWS/GCP/阿里云），24小时稳定运行：

```bash
# 使用screen或tmux保持会话
screen -S stock_screener
python main.py --daemon

# 分离会话: Ctrl+A, D
# 重新连接: screen -r stock_screener
```

### 使用systemd服务（推荐Linux）

创建服务文件 `/etc/systemd/system/stock-screener.service`:

```ini
[Unit]
Description=Stock Screener Service
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/stock_screener
ExecStart=/usr/bin/python3 /path/to/stock_screener/main.py --daemon
Restart=always

[Install]
WantedBy=multi-user.target
```

启动服务：

```bash
sudo systemctl enable stock-screener
sudo systemctl start stock-screener
sudo systemctl status stock-screener
```

## 常见问题

### Q: 数据下载失败怎么办？

A: yfinance依赖Yahoo Finance API，可能因为网络问题失败。建议：
- 检查网络连接
- 使用代理（如果在国内）
- 减少并发下载数量（修改`DataFetcher`的`max_workers`参数）

### Q: 如何修改股票池？

A: 编辑 `config/config.yaml`，修改 `data.stock_universe` 为 `sp500`, `nasdaq100` 或提供自定义列表。

### Q: 能否支持A股或港股？

A: yfinance支持全球主要市场。修改 `src/utils.py` 中的股票列表获取函数即可。例如港股添加`.HK`后缀。

### Q: 如何调整调度时间？

A: 编辑 `config/config.yaml` 中的 `scheduler.schedules`，使用24小时制时间格式。

### Q: 报告太多如何清理？

A: 运行以下代码清理30天前的报告：

```python
from src.reporter import Reporter
reporter = Reporter()
reporter.clean_old_reports(keep_days=30)
```

## 性能优化建议

1. **减少股票数量**: 如果只关注特定板块，可以自定义股票列表
2. **调整历史数据长度**: 如果策略不需要太长历史，可以减少`history_days`
3. **使用数据库**: 对于大量数据，可以替换CSV存储为SQLite或PostgreSQL
4. **并行计算**: 技术指标计算可以使用多进程加速

## 开发进度

详见 [任务跟踪文档](.specify/features/stock-screening-system/tasks_summary_cn.md)

### 已完成 ✅
- [x] **第1阶段：周趋势确认** - 多时间框架验证，减少假信号

### 进行中
- [ ] 第2阶段：信号置信度分级
- [ ] 第3阶段：回测框架 ⚠️ 最高优先级
- [ ] 第4阶段：AI集成层（存根）
- [ ] 第5阶段：集成测试与文档

## 后续扩展方向

- [ ] 添加邮件/微信/Telegram推送通知
- [ ] Web可视化界面（Flask/Dash）
- [x] ~~回测系统验证策略有效性~~ → 第3阶段进行中
- [ ] 支持更多技术指标和形态识别
- [ ] 机器学习策略集成
- [ ] 实时数据流支持

## 许可证

MIT License

## 免责声明

本系统仅供学习和研究使用，不构成任何投资建议。股市有风险，投资需谨慎。使用本系统进行投资决策的任何损失，作者概不负责。

## 技术支持

如有问题或建议，欢迎提Issue。

---

**Happy Trading! 📈**

